commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit {1}

pre-commit:
  parallel: true
  commands:
    skillcheck:
      glob: "skills/**/*"
      run: pnpm exec ts-node scripts/skillcheck.ts {staged_files}
